{
  "name": "bare-bones-bankroll",
  "version": "1.0.2",
  "author": "Logan St. John",
  "description": "Poker bankroll tracker",
  "main": "electron/main.js",
  "scripts": {
    "electron:dev": "npm run build --prefix client && electron .",
    "electron:build": "cross-env CSC_IDENTITY_AUTO_DISCOVERY=false npm run build --prefix client && electron-builder"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "electron": "^41.0.0",
    "electron-builder": "^26.8.1"
  },
  "dependencies": {
    "electron-updater": "^6.8.3"
  }
}
